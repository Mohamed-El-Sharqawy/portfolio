import Section from "@/components/ui/Section";

export default function Hero() {
  return (
    <Section id="top">
      <p className="font-mono text-xs text-zinc-500">hero — milestone 2</p>
    </Section>
  );
}
